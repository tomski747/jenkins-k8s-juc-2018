apiVersion: v1
kind: Service
metadata:
  name: {{ template "hello-jenkins.fullname" . }}
  labels:
    app: {{ template "hello-jenkins.name" . }}
    chart: {{ template "hello-jenkins.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "hello-jenkins.name" . }}
    release: {{ .Release.Name }}
